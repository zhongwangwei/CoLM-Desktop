{
  "build": {
    "beforeBuildCommand": "cargo run -q -p xtask --manifest-path ../xtask/Cargo.toml -- stage-sidecar"
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "externalBin": [
      "binaries/colm-cli"
    ],
    "resources": {
      "../../kernels": "kernels",
      "../../examples": "examples"
    }
  }
}
